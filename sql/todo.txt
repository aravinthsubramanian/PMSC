cd PMSC


1=====================================
composer install

2======================================
cp .env.example .env  ((copy [.env.example] content to [.env]))

3======================================
php artisan key:generate

4=======================================
php artisan storage:link

5======================================================
composer require spatie/laravel-permission

6==================================================================================
service provider automatically (Or) manually add config/app.php file:
'providers' => [
    // ...
    Spatie\Permission\PermissionServiceProvider::class,
];

7=========================================================================================
php artisan vendor:publish --provider="Spatie\Permission\PermissionServiceProvider"

8==============================================================
//then..................mysql config in .ENV file

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=pmsc
DB_USERNAME=root
DB_PASSWORD=

9====================================================================
php artisan optimize (or) php artisan config:clear

10=======================================================
php artisan migrate

11===================================================
php artisan db:seed

12============================================